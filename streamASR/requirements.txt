# StreamAlign dependencies.
#
# Versions are the ones this code was last validated against. SpeechBrain is
# NOT listed: it is vendored at streamASR/speechbrain and resolves from there
# whenever streamASR/ is on the path. Installing it from PyPI would shadow the
# vendored fork.
#
# Resynthesis additionally needs a CosyVoice checkout (flow + HiFT decoder),
# pointed to by COSYVOICE_ROOT; it is not pip-installable.

# --- core ---
torch==2.8.0
torchaudio==2.8.0
numpy==2.2.6
scipy==1.15.3
numba==0.63.1

# --- speechbrain runtime deps (needed by the vendored copy) ---
hyperpyyaml==1.2.3
ruamel.yaml==0.18.17
sentencepiece==0.2.0
joblib==1.5.3

# --- tokenizer / quantizer ---
vector-quantize-pytorch==1.27.20
einops==0.8.2

# --- FastEmit RNN-T loss (train_asr_word_fastemit.py) ---
nemo-toolkit==2.6.2

# --- text / LLM tokenizers ---
transformers==5.3.0
tokenizers==0.22.2
huggingface-hub==1.7.2

# --- audio io / features ---
soundfile==0.13.1
librosa==0.11.0

# --- data, logging, misc ---
pandas==2.3.3
tqdm==4.67.2
msgpack==1.2.1
matplotlib==3.10.8
wandb==0.24.1

# --- evaluation ---
# jiwer: streamASR/evaluate/ (WER, latency)
# openai-whisper: streamSLM continuation transcribes its own output
jiwer==3.1.0
openai-whisper==20250625
